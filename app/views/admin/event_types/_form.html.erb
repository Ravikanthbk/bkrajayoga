<%= form_for(@admin_event_type) do |f| %>
  <% if @admin_event_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_event_type.errors.count, "error") %> prohibited this admin_event_type from being saved:</h2>

      <ul>
      <% @admin_event_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>:
    <%= f.text_field :name, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit :class => "btn btn-primary" %>
  </div>
<% end %>
