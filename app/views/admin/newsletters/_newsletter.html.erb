      <tr class="panel panel-default" id="spec-<%= newsletter.id %>">
        <td><%= newsletter.title %></td>
        <td><%= link_to 'Show', newsletter %></td>
        <td><%= link_to 'Edit', edit_admin_newsletter_path(newsletter) %></td>
        <td><%= link_to 'Delete', newsletter, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>